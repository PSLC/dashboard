import { getSkillMastery } from 'dashboard/helpers/get-skill-mastery';
import { module, test } from 'qunit';

module('Unit | Helper | get skill mastery');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getSkillMastery([42]);
  assert.ok(result);
});
